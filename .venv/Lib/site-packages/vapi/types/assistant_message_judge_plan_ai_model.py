# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from ..core.serialization import FieldMetadata
from ..core.unchecked_base_model import UncheckedBaseModel, UnionMetadata
from .anthropic_thinking_config import AnthropicThinkingConfig
from .eval_anthropic_model_model import EvalAnthropicModelModel
from .eval_google_model_model import EvalGoogleModelModel
from .eval_open_ai_model_model import EvalOpenAiModelModel


class AssistantMessageJudgePlanAiModel_Openai(UncheckedBaseModel):
    """
    This is the model to use for the LLM-as-a-judge.
    If not provided, will default to the assistant's model.

    The instructions on how to evaluate the model output with this LLM-Judge must be passed as a system message in the messages array of the model.

    The Mock conversation can be passed to the LLM-Judge to evaluate using the prompt {{messages}} and will be evaluated as a LiquidJS Variable. To access and judge only the last message, use {{messages[-1]}}

    The LLM-Judge must respond with "pass" or "fail" and only those two responses are allowed.
    """

    provider: typing.Literal["openai"] = "openai"
    model: EvalOpenAiModelModel
    temperature: typing.Optional[float] = None
    max_tokens: typing_extensions.Annotated[
        typing.Optional[float], FieldMetadata(alias="maxTokens"), pydantic.Field(alias="maxTokens")
    ] = None
    messages: typing.List[typing.Dict[str, typing.Any]]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class AssistantMessageJudgePlanAiModel_Anthropic(UncheckedBaseModel):
    """
    This is the model to use for the LLM-as-a-judge.
    If not provided, will default to the assistant's model.

    The instructions on how to evaluate the model output with this LLM-Judge must be passed as a system message in the messages array of the model.

    The Mock conversation can be passed to the LLM-Judge to evaluate using the prompt {{messages}} and will be evaluated as a LiquidJS Variable. To access and judge only the last message, use {{messages[-1]}}

    The LLM-Judge must respond with "pass" or "fail" and only those two responses are allowed.
    """

    provider: typing.Literal["anthropic"] = "anthropic"
    model: EvalAnthropicModelModel
    thinking: typing.Optional[AnthropicThinkingConfig] = None
    temperature: typing.Optional[float] = None
    max_tokens: typing_extensions.Annotated[
        typing.Optional[float], FieldMetadata(alias="maxTokens"), pydantic.Field(alias="maxTokens")
    ] = None
    messages: typing.List[typing.Dict[str, typing.Any]]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class AssistantMessageJudgePlanAiModel_Google(UncheckedBaseModel):
    """
    This is the model to use for the LLM-as-a-judge.
    If not provided, will default to the assistant's model.

    The instructions on how to evaluate the model output with this LLM-Judge must be passed as a system message in the messages array of the model.

    The Mock conversation can be passed to the LLM-Judge to evaluate using the prompt {{messages}} and will be evaluated as a LiquidJS Variable. To access and judge only the last message, use {{messages[-1]}}

    The LLM-Judge must respond with "pass" or "fail" and only those two responses are allowed.
    """

    provider: typing.Literal["google"] = "google"
    model: EvalGoogleModelModel
    temperature: typing.Optional[float] = None
    max_tokens: typing_extensions.Annotated[
        typing.Optional[float], FieldMetadata(alias="maxTokens"), pydantic.Field(alias="maxTokens")
    ] = None
    messages: typing.List[typing.Dict[str, typing.Any]]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class AssistantMessageJudgePlanAiModel_CustomLlm(UncheckedBaseModel):
    """
    This is the model to use for the LLM-as-a-judge.
    If not provided, will default to the assistant's model.

    The instructions on how to evaluate the model output with this LLM-Judge must be passed as a system message in the messages array of the model.

    The Mock conversation can be passed to the LLM-Judge to evaluate using the prompt {{messages}} and will be evaluated as a LiquidJS Variable. To access and judge only the last message, use {{messages[-1]}}

    The LLM-Judge must respond with "pass" or "fail" and only those two responses are allowed.
    """

    provider: typing.Literal["custom-llm"] = "custom-llm"
    url: str
    headers: typing.Optional[typing.Dict[str, typing.Any]] = None
    timeout_seconds: typing_extensions.Annotated[
        typing.Optional[float], FieldMetadata(alias="timeoutSeconds"), pydantic.Field(alias="timeoutSeconds")
    ] = None
    model: str
    temperature: typing.Optional[float] = None
    max_tokens: typing_extensions.Annotated[
        typing.Optional[float], FieldMetadata(alias="maxTokens"), pydantic.Field(alias="maxTokens")
    ] = None
    messages: typing.List[typing.Dict[str, typing.Any]]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


AssistantMessageJudgePlanAiModel = typing_extensions.Annotated[
    typing.Union[
        AssistantMessageJudgePlanAiModel_Openai,
        AssistantMessageJudgePlanAiModel_Anthropic,
        AssistantMessageJudgePlanAiModel_Google,
        AssistantMessageJudgePlanAiModel_CustomLlm,
    ],
    UnionMetadata(discriminant="provider"),
]
