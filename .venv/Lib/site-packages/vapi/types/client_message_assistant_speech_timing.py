# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from ..core.serialization import FieldMetadata
from ..core.unchecked_base_model import UncheckedBaseModel, UnionMetadata
from .assistant_speech_word_timestamp import AssistantSpeechWordTimestamp


class ClientMessageAssistantSpeechTiming_WordAlignment(UncheckedBaseModel):
    """
    Optional timing metadata. Shape depends on `timing.type`:

    - `word-alignment` (ElevenLabs): per-character timing at playback
      cadence. words[] includes space entries. Best consumed by tracking
      a running character count: join timing.words, add to a char cursor,
      and highlight text up to that position. No interpolation needed.

    - `word-progress` (Minimax with voice.subtitleType: 'word'): cursor-
      based word count per TTS segment. Use wordsSpoken as the anchor,
      interpolate forward using segmentDurationMs or timing.words until
      the next event arrives.

    When absent, the event is a text-only fallback for providers without
    word-level timing (e.g. Cartesia, Deepgram, Azure). Text emits once
    per TTS chunk when audio is playing. Optionally interpolate a word
    cursor at ~3.5 words/sec between events for approximate tracking.
    """

    type: typing.Literal["word-alignment"] = "word-alignment"
    words: typing.List[str]
    words_start_times_ms: typing_extensions.Annotated[
        typing.List[float], FieldMetadata(alias="wordsStartTimesMs"), pydantic.Field(alias="wordsStartTimesMs")
    ]
    words_end_times_ms: typing_extensions.Annotated[
        typing.List[float], FieldMetadata(alias="wordsEndTimesMs"), pydantic.Field(alias="wordsEndTimesMs")
    ]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class ClientMessageAssistantSpeechTiming_WordProgress(UncheckedBaseModel):
    """
    Optional timing metadata. Shape depends on `timing.type`:

    - `word-alignment` (ElevenLabs): per-character timing at playback
      cadence. words[] includes space entries. Best consumed by tracking
      a running character count: join timing.words, add to a char cursor,
      and highlight text up to that position. No interpolation needed.

    - `word-progress` (Minimax with voice.subtitleType: 'word'): cursor-
      based word count per TTS segment. Use wordsSpoken as the anchor,
      interpolate forward using segmentDurationMs or timing.words until
      the next event arrives.

    When absent, the event is a text-only fallback for providers without
    word-level timing (e.g. Cartesia, Deepgram, Azure). Text emits once
    per TTS chunk when audio is playing. Optionally interpolate a word
    cursor at ~3.5 words/sec between events for approximate tracking.
    """

    type: typing.Literal["word-progress"] = "word-progress"
    words_spoken: typing_extensions.Annotated[
        float, FieldMetadata(alias="wordsSpoken"), pydantic.Field(alias="wordsSpoken")
    ]
    total_words: typing_extensions.Annotated[
        float, FieldMetadata(alias="totalWords"), pydantic.Field(alias="totalWords")
    ]
    segment: typing.Optional[str] = None
    segment_duration_ms: typing_extensions.Annotated[
        typing.Optional[float], FieldMetadata(alias="segmentDurationMs"), pydantic.Field(alias="segmentDurationMs")
    ] = None
    words: typing.Optional[typing.List[AssistantSpeechWordTimestamp]] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


ClientMessageAssistantSpeechTiming = typing_extensions.Annotated[
    typing.Union[ClientMessageAssistantSpeechTiming_WordAlignment, ClientMessageAssistantSpeechTiming_WordProgress],
    UnionMetadata(discriminant="type"),
]
